<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.UserMapper">

	<update id="updateUser" parameterType="userDto">
		UPDATE ssafy_member
		SET userpwd = #{userpwd}, name = #{name}, phone = #{phone}, address = #{address} 
		WHERE userid = #{userid}
	</update>

	<insert id="registerMember" parameterType="UserDto">
		insert into ssafy_member
		values (#{userid},#{username},#{userpwd},#{email} ,now())
	</insert>
	
	<select id="login" parameterType="map" resultType="UserDto">
		select userid, userpwd, name, email, joindate 
		from ssafy_member
		where userid = #{userid} and password = #{userpwd}
	</select>
	
	<delete id="deleteUser" parameterType="String">
		DELETE FROM ssafy_member
		WHERE userid = #{userid}
	</delete>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.UserMapper">

	<update id="updateUser" parameterType="userDto">
		UPDATE user
		SET password = #{password}, name = #{name}, phone = #{phone}, address = #{address} 
		WHERE userid = #{userid}
	</update>

	<insert id="registerMember" parameterType="UserDto">
		insert into user (userid, password, name, phone, address)
		values (#{userid}, #{password}, #{name}, #{phone}, #{address})
	</insert>
	
	<select id="login" parameterType="map" resultType="UserDto">
		select userid, password, name, phone, address 
		from user
		where userid = #{userid} and password = #{password}
	</select>
	
	<delete id="deleteUser" parameterType="String">
		DELETE FROM user
		WHERE userid = #{userid}
	</delete>
</mapper>